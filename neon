// app/actions.ts
"use server";
import { neon } from "@neondatabase/serverless";
import { env } from "/home/user/instagramclone/src/lib/env.ts";

export async function getData() {
    const sql = neon(env.DATABASE_URL!);
    const data = await sql`...`;
    return data;
}